# Assets Directory

This folder is available for bundled media and assets that can be imported directly into TypeScript/JavaScript modules:

```typescript
import logo from '@/assets/images/logo.png';
```

> **Note**: For static files served directly over HTTP without bundling (e.g. `<img src="/assets/images/..." />`), place them in `/public/assets/images/`.
